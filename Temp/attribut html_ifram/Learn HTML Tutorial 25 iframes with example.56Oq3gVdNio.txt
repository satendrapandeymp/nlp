hello friends I am champion my pal today we are going to learn about iframes in HTML here is it an example to show how I frameworks in HTML iframe is used to create a frame within an HTML body of desired size Here I am creating I frame by frame is created with the help of iframe tab then SRC source but HTML page we want to show in April Here I am writing table d'hôte HTML means table d'hote HTML page will be shown then we have to specify it size height and width okay then we have to close the eye cream tech this is a simple syntax for IBM tech now I'm going to show you to output here is the output this is the high frame of the desired size and this is the HTML phase if you want to show in the iframe this is all about I frame thank you		